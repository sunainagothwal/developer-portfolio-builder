node_modules/
.expo/
.expo-shared/
dist/
web-build/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.DS_Store
.env
.env.local
*.log
coverage/
.vscode/
.idea/
*.tsbuildinfo

# Native projects are regenerated by `expo prebuild` (EAS Build does this
# automatically on every cloud build). Nothing here is hand-edited, so
# committing it would only drift out of sync with app.json and the installed
# plugins over time.
/android/
/ios/

# Local build output — every test APK comes from EAS Build instead.
*.apk
*.aab
credentials.json
.claude/
